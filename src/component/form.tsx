import * as React from "react";
import { FormDef } from "../core/form_def";

interface FormProps {
  formDef: FormDef;
}

const Form: React.FC = ({ formDef }: FormProps) => {
  return <div></div>;
};

export default Form;
